-- ============================================================
-- V0001 의 개발용 SUPER 관리자 제거
-- ============================================================
-- V0001 이 부트스트랩을 위해 SUPER 관리자를 심어 뒀는데, 비밀번호('swyp-admin-1234')와 그
-- BCrypt 해시가 공개 저장소에 그대로 적혀 있다. 이 마이그레이션이 운영에서도 돌기 때문에,
-- 그 계정을 지우지 않으면 배포 즉시 누구나 SUPER 로 로그인할 수 있다.
--
-- V0001 은 이미 적용된 마이그레이션이라 수정할 수 없으므로(체크섬), 새 파일로 걷어낸다.
--
-- 해시까지 함께 비교하는 이유: 누군가 이 계정의 비밀번호를 이미 바꿔서 실제로 쓰고 있다면
-- 그건 의도적으로 만든 관리자이므로 건드리면 안 된다. 손대지 않은 시드 행만 지운다.
delete from admins
where email = 'admin@swyp.com'
  and password = '$2a$10$d/Uurstls9aEwNeY19bwL.2gNOA1MAPPse7.pHjw.P8sRvQyltPPm';

-- 로컬 개발에서 이 계정이 필요하면 db/data/dev_seed_admin.sql 을 직접 넣는다(Flyway 경로 밖).
-- 운영 첫 관리자는 db/data/README.md 의 부트스트랩 절차를 따른다.
